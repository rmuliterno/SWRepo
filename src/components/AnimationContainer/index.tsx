import React from 'react';

import { Container } from './styles';

const AnimationContainer: React.FC = ({ children }) => (
  <Container>
    {children}
  </Container>
);

export default AnimationContainer;
